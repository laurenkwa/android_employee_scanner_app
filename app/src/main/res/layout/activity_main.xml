<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity" android:orientation="vertical">
    <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="match_parent" android:layout_weight="1" android:gravity="center"
            >
        <ImageButton
                android:layout_width="wrap_content"
                android:layout_height="70dp" app:srcCompat="@android:drawable/ic_search_category_default"
                android:id="@+id/imageButton3" android:layout_weight="1" android:onClick="onSearch"
                android:tooltipText="@string/search"
                android:paddingBottom="10dp"
                android:paddingLeft="10dp"
                android:paddingRight="10dp"
                android:paddingTop="10dp"
                android:scaleType="fitCenter"/>
        <ImageButton
                android:layout_width="wrap_content"
                android:layout_height="70dp" app:srcCompat="@android:drawable/ic_menu_camera"
                android:id="@+id/imageButton2" android:layout_weight="1" android:onClick="onScan"
                android:tooltipText="@string/scan"
                android:paddingBottom="10dp"
                android:paddingLeft="10dp"
                android:paddingRight="10dp"
                android:paddingTop="10dp"
                android:scaleType="fitCenter"/>
    </LinearLayout>

</LinearLayout>